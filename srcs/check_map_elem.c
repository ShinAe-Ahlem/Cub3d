#include "../includes/game.h"

void checkMapElement(t_game *game)
{
    checkExportTextures(game);
    // check_color_format(game.mapfile);
    // check_map(game.mapfile);
    // divide_elem(game);
}
